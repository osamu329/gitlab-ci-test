stages:
    - test
    - build
test:
    type: test
    script:
        - echo PATH=$PATH
        - export PATH=$PATH:$GOROOT/bin
        - echo PATH=$PATH
        - go test ./go/...
    tags:
        - docker

build:
    script:
        - go build ./go/...
    artifacts:
        paths:
            - app.exe
        expire_in: 1 week

